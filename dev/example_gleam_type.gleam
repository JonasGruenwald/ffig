//// Here I'm just declaring some types that I should then be able to use from typescript

pub type MyExampleType {
  ExampleOne(wibble: String)
  ExampleTwo(wobble: Int)
}

pub type MyGenericExampleType(variable) {
  Wobblerone(variable)
}
